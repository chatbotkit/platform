# Dependency Stubs

This directory contains lightweight local packages that stand in for optional,
native, or incompatible dependencies when their functionality is not used by
the platform. Each stub documents what it replaces and which behavior must not
be called at runtime.
