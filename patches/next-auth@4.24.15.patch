diff --git a/core/routes/session.js b/core/routes/session.js
index d23383d..7ce2119 100644
--- a/core/routes/session.js
+++ b/core/routes/session.js
@@ -117,11 +117,16 @@ async function session(params) {
         }
         const sessionPayload = await callbacks.session({
           session: {
+            // @note added the raw session because we needed to construct the final session object from the information in the db
+
+            ...session,
+
             user: {
               name: user.name,
               email: user.email,
               image: user.image
             },
+
             expires: session.expires.toISOString()
           },
           user,
diff --git a/credits.js b/credits.js
new file mode 100644
index 0000000..f9da0f5
--- /dev/null
+++ b/credits.js
@@ -0,0 +1,3 @@
+module.exports = {
+    patchedBy: 'chatbotkit'
+}
\ No newline at end of file
diff --git a/package.json b/package.json
index 3b0334c..6a5d95f 100644
--- a/package.json
+++ b/package.json
@@ -57,6 +57,9 @@
     "./providers/*": {
       "types": "./providers/*.d.ts",
       "default": "./providers/*.js"
+    },
+    "./credits": {
+      "default": "./credits.js"
     }
   },
   "scripts": {
diff --git a/react/index.js b/react/index.js
index d043a15..6b31be1 100644
--- a/react/index.js
+++ b/react/index.js
@@ -264,7 +264,9 @@ function _signIn() {
           if (url.includes("#")) window.location.reload();
           return _context6.abrupt("return");
         case 42:
-          error = new URL(data.url).searchParams.get("error");
+          // @note fixed because the data.url could be relative
+
+          error = new URL(data.url, window.location.origin).searchParams.get("error");
           if (!res.ok) {
             _context6.next = 46;
             break;
