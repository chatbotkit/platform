# Dependency Patches

This directory contains patches applied to third-party dependencies by pnpm.
Each patch is referenced from the root `package.json` and should remain scoped
to behavior that cannot be addressed through normal dependency configuration.
