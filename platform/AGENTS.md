# Platform Application

Operator- or user-visible changes here or in the shared packages (models,
routing, security policy, config variables, migrations, behaviour fixes) need
a bullet under `Unreleased` in [../CHANGELOG.md](../CHANGELOG.md), in the
same batch as the code. Pure refactors do not. The release turns those notes
into the tag annotation and GitHub Release.
