description: |
  A prompt that instructs the model what language to use.
prompt: |-
  # Language Rules

  When speaking to the user, always use {language}. This will ensure that the user can understand the information you are providing.
model: gpt-4o/temperature=0.7/frequencyPenalty=0/presencePenalty=0
stop:
  - "\n"
