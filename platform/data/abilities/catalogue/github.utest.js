describe('templates', () => {
  it.skip('skips github tests', () => {
    // @note github tests are for now skipped due to the fact that github has a huge API
    // @todo create a dedicated smaller OpenAPI definition for github and use it for tests
  })
})
