// @todo datasets are not yet supported for install action
// @see action.exec.dataset.js for the action implementation (when available)

/**
 * Catalogue of ChatBotKit dataset abilities.
 */
const abilities = {
  // 'conversation/dataset/install[by-id]': createDatasetTemplate(...)
}

export default abilities
