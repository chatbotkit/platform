query availableSharedSecrets {
  secrets(last: 100, kind: [shared]) {
    edges {
      node {
        id
        name
        description

        verification {
          status
          action {
            type
            url
          }
        }
      }
    }
  }
}
