# This query fetches template secrets from all related accounts, i.e. other
# accounts that share the same parent organization as the current account.

query templateSecrets {
  relatedSecrets(last: 100, visibility: [protected]) {
    edges {
      node {
        id
        name
        description
      }
    }
  }
}
