import type { Prisma } from '@chatbotkit-dev/db/client'

// @note Prisma.dmmf was removed in Prisma 7. The DMMF is no longer exposed at
// runtime. If getTaggedFields is needed in the future, it should be implemented
// as a build-time codegen step that reads the schema file directly.

export type { Prisma }
