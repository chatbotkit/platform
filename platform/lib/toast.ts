import toast from 'react-hot-toast'

// @todo automatically trim the messages to length to avoid overflow issues
// @note we already do this on specific places but we need specific solution as
// a general purpose toast handler

export default toast
