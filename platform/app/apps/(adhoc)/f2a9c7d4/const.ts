export { CONTACT_NAMESPACE } from '../../const'

export const APP_NAME = 'f2a9c7d4'

// @note all per-user projects live under this prefix inside the user's space
export const PROJECTS_PREFIX = 'projects'

// @note the file that holds the project graph (nodes/edges) within a project
export const PROJECT_FILE = 'project.json'

// @note generated images (assets) are stored alongside the project file
export const ASSETS_DIR = 'assets'
