# @chatbotkit-dev/json

Forgiving JSON parsing and stringifying.

`relaxedJsonParse` accepts output a model produced rather than strictly valid JSON. `tryParse` and
`tryStringify` return a fallback instead of throwing.

Extracted from `platform/lib/json.ts`.
