# @chatbotkit-dev/struct

Making values safe to serialise.

`makeJsonSafe` strips what JSON cannot carry, `stripEmpty` removes empty members, and `SafeJson`
wraps a value that has already been made safe.

Extracted from `platform/lib/struct.ts`. Zero dependencies.
