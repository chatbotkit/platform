# @chatbotkit-dev/env

Which environment the process is running in.

Exports `isDevelopment`, `isStaging`, `isProduction` and `isTest`, resolved once
from the provider-neutral `NODE_ENV` and `TARGET_ENV` variables.

Extracted from `platform/lib/env.ts`. Zero dependencies.
