// @note export the builder and context type for external use
export { builder, type Context } from './builder'

// @note export HTTP field helper
export { createHttpField, type HttpFieldConfig } from './http-field'

// @note export service namespaces
export { NotionNamespace } from './notion'
export { SlackNamespace } from './slack'

// @note export the combined schema
export { schema } from './schema'
