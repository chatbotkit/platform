# @chatbotkit-dev/partners-spec

The contract for the partner catalogue.

Types only. Separate from [`@chatbotkit-dev/partners`](../partners) because that
package is swappable, and an implementation cannot import the package it replaces.
