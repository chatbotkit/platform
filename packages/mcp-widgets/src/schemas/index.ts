/**
 * Zod Schemas for Runtime Validation
 *
 * Import from 'mcp-widgets/schemas' to access validation utilities.
 * This module includes Zod as a dependency.
 *
 * For types-only imports (no Zod bundled), use the main entry point:
 * ```ts
 * import type { ManifestMetadata } from 'mcp-widgets'
 * ```
 *
 * For validation:
 * ```ts
 * import { ManifestMetadataSchema, parseManifest } from 'mcp-widgets/schemas'
 * ```
 */

export * from './manifest'
