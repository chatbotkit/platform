# @chatbotkit-dev/math

Small numeric helpers with no opinion about the platform: rounding to a
multiple, clamping to a range. Kept as a package so configuration data (which
must not reach into the application's `lib/`) and the application can share
them.
