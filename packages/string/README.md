# @chatbotkit-dev/string

String manipulation used across the platform.

Extracted from `platform/lib/string.js`. Still JavaScript, because it was never type checked in
the application either; converting it will surface real errors rather than only adding annotations.
